platform :ios, '9.0'

target 'Authentification Firebase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Authentification Firebase
  pod 'Firebase'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  

end
